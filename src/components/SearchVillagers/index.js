import { TextField, Autocomplete } from '@material-ui/core'

export default function SearchVillagers(props) {
  return (
    <div>
      <TextField type="search" value="Search Not Available Yet" style={{width: '100%', margin: '1rem 0'}} disabled />
    </div>
  )
}
